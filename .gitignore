# Ключи к API. В репозиторий не едут никогда.
.env

.venv/
__pycache__/
*.pyc

# Всё, что лежит в data, программа либо сделала сама, либо скачала:
# база матчей (почти 2 ГБ), иконки с CDN Valve, snapshot, настройки окна
# и снимки пакетов от игры. Последние вдобавок личные — в них номера
# аккаунтов и ники из настоящих матчей.
backend/data/

# Сборка
build/
dist/
installer/Output/

# Настройки редактора
.kilo/
